import { Header } from '@/components/header';
import { HomeHero } from '@/features/home-hero';

const HomePage = () => {
  return (
    <main>
      <Header />
      <HomeHero />
    </main>
  );
};

export default HomePage;
