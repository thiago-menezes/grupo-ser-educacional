/**
 * seo router
 */

import { factories } from '@strapi/strapi';

// Content type 'api::seo.seo' not yet in generated types. Run 'npm run strapi build' to regenerate.
export default factories.createCoreRouter('api::seo.seo' as any);
